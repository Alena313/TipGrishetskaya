    var y = prompt("введите первый катет");
    var z = prompt("введите второй катет");
    y=Number(y);
    z=Number(z);
    var x=Math.sqrt(Math.pow(y,2)+Math.pow(z,2));
    var result = confirm(x+ "\n" + "Все верно?")
   

